- do login (cache auth token)
- command to fetch all released DOU dates
- command to download and sync local and remote files
- command to diff every xml ordered by release date
- command to index every xml so we can find TERMS

- schedule command to fetch and process all missing releases from latest saved

- create a visual interface to show every DOU release diff
